server:
  port: 9002
spring:
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848

#        监控的端点
management:
  endpoints:
    web:
      exposure:
        include: '*'